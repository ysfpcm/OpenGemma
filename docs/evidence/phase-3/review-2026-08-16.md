# Phase 3 review addendum — 2026-08-16

## Review status

Phase 3 is **ACCEPTED** by Marc on 2026-08-16 (America/New_York). This
addendum records the completed audit and the explicit acceptance decision.

## Inventory

- Promised deliverable: a fail-closed Guardian Kernel and verified action
  runtime shared by registered Home Assistant and Codex approval paths.
- Implementation: `src/openjarvis/guardian/`, the Phase 0 action ledger and
  runtime, the Guardian API, the registered reversible Home Assistant adapter,
  the native Codex approval bridge, and the causal-timeline UI/API path.
- Evidence: deterministic fixture actions, denials, stale and contradictory
  preconditions, revocation, emergency stop, independent verification,
  restart persistence, immutable audit records, native Codex handoff, and
  local API validation.
- Dependencies: Phase 0 action lifecycle; Phase 1/2 Codex observer and
  mission-control records; no live external service was used by this review.
- Deliberate limits: only reversible Home Assistant `turn_on`/`turn_off`
  actions are registered; Codex approval records do not claim to verify later
  command or file effects.

## Changes made during this review

- Replayed proposals now remain idempotent, while proposal/idempotency-key
  collisions fail closed without mutating the stored action.
- Invalid JSON-number booleans, malformed executor outcomes, precondition
  exceptions, future/malformed observations, and timeout exceptions fail
  closed with truthful ledger states.
- Home Assistant transport timeouts become `needs_attention` and are never
  automatically retried.
- Guardian API identifiers now reject empty values at request validation.
- Removed the unused `_ATTRIBUTE_ACTIONS` constant from the Home Assistant
  tool. Other private write helpers were retained as compatibility candidates.

## Cleanup review

- Removed: `_ATTRIBUTE_ACTIONS`, proven unused by repository-wide reference
  search after direct Home Assistant writes moved behind Guardian adapters.
- Retained: `_find_entity` and the private write/verification helpers because
  their compatibility surface is uncertain even though no repository caller
  currently uses them.
- Deferred: broader removal of those private helpers, authentication-policy
  redesign, and any live adapter expansion; each would exceed this focused
  cleanup or require a compatibility decision.

## Current verification

- Phase 3 and neighboring focused suite: 81 passed.
- Phase 1/2 Codex regression suite: 18 passed.
- Complete `tests/acceptance` regression: 111 passed.
- Ruff lint, Ruff format check, Python compilation, frontend build, and Vitest
  all passed; Vitest reported 6 tests passed.

## Deferred follow-up

No live Home Assistant or live Codex App Server effect was performed. This is
intentional for this isolated review and does not block the frozen fixture
acceptance scenario. Live operational validation remains separate from this
Phase 3 review.
